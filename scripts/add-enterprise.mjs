import { createClient } from '@supabase/supabase-js'; import dotenv from 'dotenv'; dotenv.config({ path: '.env.local' }); const supabase = createClient(process.env.NEXT_PUBLIC_SUPABASE_URL, process.env.SUPABASE_SERVICE_ROLE_KEY || process.env.SUPABASE_SECRET_KEY); async function run() { const { data: existing } = await supabase.from('plans').select('id').eq('slug', 'enterprise').single(); if (existing) { console.log('Enterprise plan already exists'); return; } const { data: plan, error: planError } = await supabase.from('plans').insert({ slug: 'enterprise', name_en: 'Enterprise', name_es: 'Enterprise', description_en: 'Ultimate power for teams', description_es: 'Poder absoluto para equipos', price_monthly: 197.00, stripe_price_id: 'price_enterprise_test', features_en: ['All apps included', 'Unlimited generations', '24/7 Priority support', 'Early access to new apps', 'Personalized onboarding session'], features_es: ['Todas las apps incluidas', 'Generaciones ilimitadas', 'Soporte prioritario 24/7', 'Acceso anticipado a nuevas apps', 'Sesión de onboarding personalizada'], is_active: true, sort_order: 4 }).select('id').single(); if (planError) { console.error('Plan err', planError); return; } console.log('Plan created with ID:', plan.id); const { data: apps } = await supabase.from('micro_apps').select('id'); const planAppsToInsert = apps.map(app => ({ plan_id: plan.id, app_id: app.id })); const { error: paError } = await supabase.from('plan_apps').insert(planAppsToInsert); if (paError) { console.error('PlanApps err', paError); } else { console.log('All apps assigned to enterprise!'); } } run();
